## Linked work

Fixes #

## Reproduction and verification

- [ ] I reproduced each linked bug on this branch before changing code, or marked the issue `status:refuted` with a regression PIN test.
- [ ] `npm run check`
- [ ] `npm test`
- [ ] `npm run build`
- [ ] `node dist/cli.js check --diff`
- [ ] `node dist/cli.js verify`

Paste the actual command output or a link to the Actions run:

```text

```

## Clause provenance

List every changed non-spec hunk as `file:start-end → specs/path.md#C123`, or record it below as an explicit unmapped decision.

```text

```

## Unmapped decisions

- [ ] No changed hunk is unmapped.
- [ ] Every unmapped hunk is explicitly acknowledged below with its range and reason.

```text

```

## Risk gate

- [ ] This PR changes no unsafe, high-risk, irreversible, security, money, migration, or concurrency path.
- [ ] This PR needs human review because it changes one of those paths; I applied the `risk:high` label.
